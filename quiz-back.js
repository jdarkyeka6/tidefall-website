(()=>{const KEY='tidefall-answer-history:'+location.pathname;let replaying=false;const getHistory=()=>{try{return JSON.parse(sessionStorage.getItem(KEY)||'[]')}catch{return[]}};const setHistory=h=>sessionStorage.setItem(KEY,JSON.stringify(h));function findAnswer(label){const buttons=[...document.querySelectorAll('button')].filter(b=>b.id!=='tidefallUndoAnswer'&&b.offsetParent!==null);return buttons.find(b=>b.textContent.trim()===label)}function makeButton(){if(document.getElementById('tidefallUndoAnswer'))return;const b=document.createElement('button');b.id='tidefallUndoAnswer';b.type='button';b.textContent='← Back';b.setAttribute('aria-label','Go back one answer');Object.assign(b.style,{position:'fixed',left:'18px',bottom:'18px',zIndex:'99999',padding:'11px 16px',borderRadius:'999px',border:'1px solid rgba(255,255,255,.28)',background:'rgba(7,19,31,.94)',color:'#fff',font:'inherit',fontWeight:'700',cursor:'pointer',boxShadow:'0 10px 30px rgba(0,0,0,.35)',backdropFilter:'blur(12px)'});b.onclick=()=>{const h=getHistory();if(!h.length)return;h.pop();setHistory(h);sessionStorage.setItem(KEY+':replay','1');location.reload()};document.body.appendChild(b);update()}function update(){const b=document.getElementById('tidefallUndoAnswer');if(b){const has=getHistory().length>0;b.style.display=has?'block':'none'}}document.addEventListener('click',e=>{if(replaying)return;const b=e.target.closest('button');if(!b||b.id==='tidefallUndoAnswer')return;const container=b.closest('.answers,.ans,#answers,#a');if(!container)return;const h=getHistory();h.push(b.textContent.trim());setHistory(h);setTimeout(update,0)},true);async function replay(){if(sessionStorage.getItem(KEY+':replay')!=='1')return;sessionStorage.removeItem(KEY+':replay');const h=getHistory();replaying=true;for(const label of h){await new Promise(r=>setTimeout(r,30));const b=findAnswer(label);if(!b)break;b.click()}replaying=false;update()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>{makeButton();replay()});else{makeButton();replay()}})();